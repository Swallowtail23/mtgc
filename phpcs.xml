<?xml version="1.0"?>
<ruleset name="MTG Collection PHPCS">
    <description>
        PSR-12-ish baseline for the MTG Collection app,
        with PHP 8.2-8.4 compatibility checks and project exclusions.
    </description>

    <!-- Scan the whole project -->
    <file>.</file>

    <!-- File extensions -->
    <arg name="extensions" value="php,inc,phtml"/>
    <arg name="colors"/>
    <arg name="parallel" value="4"/>

    <!-- Global excludes -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>includes/analyticstracking.php</exclude-pattern>

    <!-- CLI / maintenance paths -->
    <exclude-pattern>*/bulk/*</exclude-pattern>
    <exclude-pattern>*/setup/*</exclude-pattern>
    <exclude-pattern>*/cli/*</exclude-pattern>
    <exclude-pattern>*/bin/*</exclude-pattern>
    <exclude-pattern>*/scripts/*</exclude-pattern>
    <exclude-pattern>*/console/*</exclude-pattern>

    <!--
        Base coding standard.
        The project intentionally uses alternative control-structure syntax in PHP templates and legacy entrypoints.
        App classes are namespaced, but procedural entrypoints and test fixtures are allowed to define functions and
        side effects.
    -->
    <rule ref="PSR12">
        <exclude name="Generic.ControlStructures.AlternativeControlStructure.NotAllowed"/>
        <exclude name="Generic.ControlStructures.InlineControlStructure.NotAllowed"/>
        <exclude name="PSR1.Classes.ClassDeclaration.MissingNamespace"/>
        <exclude name="PSR1.Files.SideEffects.FoundWithSymbols"/>
    </rule>

    <!-- Relax test fixtures -->
    <rule ref="PSR1.Classes.ClassDeclaration.MultipleClasses">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
    <rule ref="PSR1.Methods.CamelCapsMethodName.NotCamelCaps">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
    <rule ref="Generic.Files.LineLength">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>

    <!-- PHP compatibility -->
    <rule ref="PHPCompatibility"/>
    <config name="testVersion" value="8.2-8.4"/>
</ruleset>
