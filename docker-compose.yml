services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./cardimg:/mnt/data/cardimg
      - ./opt/mtg/mtg-new.ini:/opt/mtg/mtg-new.ini
      - ./setup/mtgc_ctr.conf:/etc/apache2/sites-available/mtgc.conf:ro
    depends_on:
      - db
  db:
    build:
      context: .
      dockerfile: Dockerfile.mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: mtg
      MYSQL_USER: mtg
      MYSQL_PASSWORD: mtgpass
    volumes:
      - db-data:/var/lib/mysql
      - ./setup/mtg_new.sql:/docker-entrypoint-initdb.d/mtg_new.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
volumes:
  db-data:
