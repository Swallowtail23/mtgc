# Replace __BASE_DIR__ with the path chosen during docker-init (e.g. /home/user/mtgc)
__BASE_DIR__/mtgapp.log {
    daily
    rotate 14
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
}
