# Replace __BASE_DIR__ with the path chosen during docker-init (e.g. /home/user/mtgc)
__BASE_DIR__/*.log {
    su www-data www-data
    daily
    rotate 14
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
}
